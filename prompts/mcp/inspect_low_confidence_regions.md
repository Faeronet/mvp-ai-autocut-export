# inspect_low_confidence_regions

Open `job://{job_id}/report` and inspect `failed_pages` and per-page confidence. Cross-check `job://{job_id}/page/{page_id}/intermediate` for OCR `review_required` flags.
